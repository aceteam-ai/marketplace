# Email Nodes

Email workflow nodes for AceTeam: SMTP send, Gmail search, Gmail drafts.

## Environment Variables

- `SMTP_USERNAME` / `SMTP_PASSWORD` — for SmtpSend node
- `GMAIL_ACCESS_TOKEN` — for GmailSearch and GmailDraft nodes
